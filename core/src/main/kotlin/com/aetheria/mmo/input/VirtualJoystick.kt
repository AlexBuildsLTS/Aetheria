package com.aetheria.mmo.input

object VirtualJoystick {
    // Simple placeholder to stop the crash.
    // We will hook this up to the UI stage later.
    fun getKnobX(): Float = 0f
    fun getKnobY(): Float = 0f
}